# Cellpose environment — Python 3.11 (system install)
# Installed at: C:\Program Files\Python311\
#
# INSTALL INSTRUCTIONS (run from an elevated PowerShell as Python 3.11 pip):
#
#   Step 1 — PyTorch with CUDA 12.8 (must be done first, before cellpose):
#     py -3.11 -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
#   Step 2 — everything else:
#     py -3.11 -m pip install -r requirements_cellpose.txt
#
# NOTES:
#   - numpy is intentionally unpinned: cellpose 3.x requires numpy<2.1,
#     so pip will choose a compatible version automatically.
#   - torch/torchvision are NOT listed here because they require a custom
#     index URL (--index-url); install them in Step 1 above.
#   - Tested on Python 3.11 / CUDA 12.8 / RTX 3050 (driver 596.08).

cellpose==3.1.1.3
numba==0.65.1
llvmlite==0.47.0
scikit-image==0.26.0
scipy==1.17.1
imageio==2.37.3
tifffile==2026.3.3
pillow==12.2.0
opencv-python-headless==4.13.0.92
tqdm==4.67.3
natsort==8.4.0
roifile==2026.2.10
fastremap==1.19.0
fill_voids==2.1.2
imagecodecs==2026.3.6
colorama==0.4.6
